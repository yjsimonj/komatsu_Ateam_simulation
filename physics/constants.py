"""물리 상수 및 재질별 기본값 테이블 (PRD §12.2).

이 모듈은 모든 물리 계산의 단일 상수 출처(single source of truth)다.
임의의 매직넘버를 코드 곳곳에 박지 말고 반드시 여기서 import 한다.
"""

from __future__ import annotations

# 중력가속도 [m/s^2] (PRD §2.1, §12.2)
G = 9.81

# ---------------------------------------------------------------------------
# 재질 밀도 [kg/m^3] (PRD §12.2)
# ---------------------------------------------------------------------------
DENSITY = {
    "wood": 600.0,      # 나무 ~600
    "cast_iron": 7200.0,  # 주철 ~7200
    "brass": 8500.0,    # 황동 ~8500
    "steel": 7850.0,    # 강철 ~7850
}

# ---------------------------------------------------------------------------
# 팁-바닥 마찰계수 μ (상대 비교용, PRD §12.2)
# (min, max) 범위. 슬라이더 기본 제한은 presets/*.py 에서 다룬다.
# ---------------------------------------------------------------------------
FRICTION = {
    "steel_smooth": (0.05, 0.15),  # 강철-매끈바닥
    "wood": (0.25, 0.40),          # 나무
    "plastic": (0.20, 0.35),       # 플라스틱
}

# ---------------------------------------------------------------------------
# 수치 안정성 파라미터 (PRD §6.1.2)
# ---------------------------------------------------------------------------
# sin^2(theta) 분모 보호 임계값. |sin(theta)| < SIN_EPS 면 phi_dot 항을 보호.
SIN_EPS = 1.0e-4

# 권장 물리 dt 상한 [s] (고속 자전 안정성, PRD §3.3 / §6.1.2)
DEFAULT_PHYSICS_DT = 5.0e-4

# 쓰러짐 판정 각도 [rad] (기본 90deg, PRD §6.1.3)
import math
THETA_FALL = math.pi / 2.0

# 직립(upright) 유지 판정 임계 각도 [rad] (직립시간 측정용, PRD §6.1.3)
# theta 가 이 값 이하로 유지된 시간을 "직립시간"으로 본다.
THETA_UPRIGHT = math.radians(20.0)
